CREATE TABLE psa_users (id SERIAL PRIMARY KEY, name TEXT NOT NULL, email TEXT UNIQUE NOT NULL, password_hash TEXT NOT NULL DEFAULT '123456', role TEXT NOT NULL, avatar TEXT NOT NULL DEFAULT '', progress INTEGER DEFAULT 0, courses_completed INTEGER DEFAULT 0, created_at TIMESTAMPTZ DEFAULT NOW());